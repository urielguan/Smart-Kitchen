# 智慧厨房管理平台技术方案文档

## 文档信息

| 项目 | 内容 |
|---|---|
| 文档名称 | 智慧厨房管理平台技术方案文档 |
| 文档版本 | v1.0 |
| 创建日期 | 2026-03-17 |
| 核心聚焦 | 前后端技术栈选型与论证 |
| 适用范围 | 系统架构设计、开发实施、运维部署 |

---

## 1. 文档概述

### 1.1 适配业务

本文档基于以下核心业务文档进行技术方案设计：

| 业务文档 | 版本 | 核心内容 |
|---|---|---|
| 智慧厨房管理平台 -标准PRD | v1.0 | 业务需求、功能规格、用户场景 |
| 智能食安管理平台功能清单 | v1.0 | 功能模块、优先级、需求说明 |
| 数据库设计-智慧厨房管理平台 | v1.0 | 表结构设计、字段定义、索引策略 |
| API设计文档-智慧厨房管理平台 | v1.0 | 接口定义、认证规范、响应格式 |

### 1.2 业务模块概览

根据PRD和功能清单，系统涵盖以下核心业务模块：

| 模块分类 | 业务模块 | 核心功能 |
|---|---|---|
| **供应链管理** | 采购管理 | 供应商管理、采购计划、采购订单 |
| **仓储管理** | 仓储管理 | 物料库存、入出库管理、盘点管理、AI需求预测 |
| **生产管理** | 菜谱营养管理 | 菜谱库、菜谱计划、AI营养分析 |
| | 烹饪记录管理 | 烹饪任务、温度监控、AI烹饪监控 |
| **食品安全** | 留样管理 | 留样记录、AI质量评估、销样管理 |
| | 智能人脸晨检 | 人脸识别、体温检测、健康证校验 |
| | 烹饪监管管理 | 实时监控、AI违规识别、人员行为分析 |
| **设备管理** | 设备管理 | 设备档案、温度传感器、气体监测 |
| | AI告警管理 | 告警分级、告警处理、策略配置 |
| **组织管理** | 组织管理 | 组织架构、成员管理 |
| | 员工管理 | 员工档案、健康证管理 |
| | 角色权限管理 | RBAC权限、数据权限 |
| **数据分析** | 数据监管 | 数据看板、趋势分析 |
| | 评价管理 | 用餐评价、申诉反馈 |

### 1.3 核心目标

本文档核心目标为：

1. **明确技术选型**：为各业务模块提供统一的技术栈选型方案
2. **论证选型依据**：从技术优势、业务适配性、团队能力等维度论证选型合理性
3. **指导开发实施**：为开发团队提供技术规范和最佳实践
4. **支撑运维部署**：为运维团队提供部署架构和配置规范

---

## 2. 后端技术栈选型与论证

### 2.1 核心框架选型

#### 2.1.1 Spring Boot 3.2 + Spring Cloud 2023 + Spring Cloud Alibaba 2023

**选型理由**

| 维度 | 选型理由 |
|---|---|
| **技术演进** | Spring Boot 3.2基于Java 17，是Spring生态系统最新LTS版本，提供长期支持 |
| **微服务架构** | 支持微服务拆分，将采购、仓储、菜谱、烹饪、留样、设备等模块独立部署 |
| **云原生适配** | 原生支持Kubernetes、Docker等云原生技术，适配容器化部署 |
| **生态兼容** | Spring Cloud Alibaba 2023完美适配Nacos 2.x，满足服务注册与配置中心需求 |

**业务适配性**

| 业务场景 | 技术支撑 |
|---|---|
| 多服务独立部署 | 支持scm-service、wms-service、recipe-service、cook-service等独立微服务 |
| API网关路由 | Spring Cloud Gateway提供统一入口，支持`/api/v1/scm/**`、`/api/v1/wms/**`等服务路由 |
| 服务间调用 | 支持服务间HTTP通信，配合OpenFeign实现声明式调用 |
| 配置中心 | Nacos统一管理数据库连接、Redis配置、业务参数等 |

**技术优势**

```
┌─────────────────────────────────────────────────────────────┐
│                    Spring Boot 3.2 核心优势                   │
├─────────────────────────────────────────────────────────────┤
│ 1. Java 17 基线支持                                          │
│    - Records（简化DTO定义）                                   │
│    - Sealed Classes（领域模型约束）                           │
│    - Pattern Matching（业务逻辑简化）                         │
│                                                              │
│ 2. AOT编译与Native Image                                      │
│    - 启动时间优化（适配JVM Xms256m-Xmx512m资源限制）          │
│    - 内存占用降低                                             │
│                                                              │
│ 3. 可观测性增强                                               │
│    - Micrometer Observation API                              │
│    - 分布式追踪集成（适配API文档中的traceId）                 │
│                                                              │
│ 4. 虚拟线程预览（Project Loom）                               │
│    - 高并发场景优化（采购订单、库存更新等）                    │
└─────────────────────────────────────────────────────────────┘
```

#### 2.1.2 版本兼容矩阵

| 组件 | 版本 | 兼容性说明 |
|---|---|---|
| Spring Boot | 3.2.x | 核心框架 |
| Spring Cloud | 2023.0.x | 微服务组件套件 |
| Spring Cloud Alibaba | 2023.0.x | 阿里巴巴微服务组件 |
| Nacos | 2.3.x | 配置中心/注册中心（地址：192.168.31.4:8848） |
| Java | 17 | 运行时环境 |

### 2.2 数据库选型

#### 2.2.1 MySQL 8.0

**选型理由**

| 维度 | 选型理由 |
|---|---|
| **技术成熟度** | MySQL 8.0是当前最成熟的开源关系型数据库，社区活跃，文档完善 |
| **MyBatis Plus适配** | 完美适配MyBatis Plus 3.5.x，主键策略、分页插件、条件构造器原生支持 |
| **共享数据库架构** | 采用表名前缀隔离策略（scm_、wms_、recipe_等），支持微服务共享单库 |
| **Docker部署** | 官方提供MySQL 8.0 Docker镜像，适配Docker Compose部署方案 |

**业务适配场景**

| 业务模块 | 数据库应用 | 表命名示例 |
|---|---|---|
| 采购管理 | 供应商、采购计划、采购订单 | scm_supplier、scm_purchase_plan、scm_purchase_order |
| 仓储管理 | 物料、库存、入出库、盘点 | wms_material、wms_stock、wms_inbound_order、wms_stocktake_order |
| 菜谱管理 | 菜谱、菜谱计划、食材 | recipe_info、recipe_plan、recipe_plan_item |
| 烹饪管理 | 烹饪任务、温度记录 | cook_task、cook_temperature_record |
| 留样管理 | 留样记录、销样记录 | sample_record、sample_dispose_record |
| 设备管理 | 设备信息、告警记录 | device_info、device_alert |

**MySQL 8.0 核心特性应用**

| 特性 | 应用场景 | 业务价值 |
|---|---|---|
| InnoDB增强 | 高并发采购订单、库存更新 | 更高并发性能、更快DDL操作 |
| JSON字段 | 设备配置、告警详情、审计日志 | 灵活存储非结构化数据 |
| 窗口函数 | 库存趋势、营养分析、成本统计 | 简化复杂统计SQL |
| CTE递归查询 | 多级组织架构查询 | 高效查询组织树 |
| UTF8MB4 | 用户评价、反馈内容 | 完整Unicode支持（含Emoji） |

### 2.3 ORM框架选型

#### 2.3.1 MyBatis Plus

**选型理由**

| 维度 | 选型理由 |
|---|---|
| **开发效率** | 自动生成CRUD操作，减少重复代码，提升开发效率30%+ |
| **灵活查询** | 条件构造器支持复杂查询，适配API设计文档中的多条件筛选 |
| **分页插件** | 原生支持MySQL 8.0分页，适配统一分页响应格式 |
| **Lombok集成** | 配合Lombok注解简化Entity、DTO定义 |

**业务适配场景**

| 业务场景 | MyBatis Plus应用 |
|---|---|
| 列表查询 | 条件构造器实现多条件筛选（如员工列表按姓名、工号、组织筛选） |
| 分页查询 | PageHelper插件实现统一分页（pageNum、pageSize） |
| 批量操作 | saveBatch、updateBatchById实现批量入出库 |
| 逻辑删除 | @TableLogic注解实现软删除（deleted_at字段） |
| 自动填充 | @TableField(fill = FieldFill.INSERT)自动填充创建时间 |

**代码示例**

```java
// Entity定义（配合Lombok）
@Data
@TableName("wms_material")
public class Material {
    @TableId(type = IdType.AUTO)
    private Long id;

    private String materialCode;
    private String materialName;
    private String materialSpec;
    private String materialUnit;
    private String materialCategory;
    private String storageRequirement;
    private Integer shelfLifeDays;
    private Integer expiryWarningDays;
    private Integer alertDays;

    @TableField(fill = FieldFill.INSERT)
    private LocalDateTime createdAt;

    @TableField(fill = FieldFill.INSERT_UPDATE)
    private LocalDateTime updatedAt;

    @TableLogic
    private LocalDateTime deletedAt;
}

// Service层查询示例
public PageResult<MaterialVO> listMaterials(MaterialQueryDTO query) {
    LambdaQueryWrapper<Material> wrapper = new LambdaQueryWrapper<>();
    wrapper.like(StringUtils.hasText(query.getMaterialName()), Material::getMaterialName, query.getMaterialName())
           .eq(StringUtils.hasText(query.getMaterialCategory()), Material::getMaterialCategory, query.getMaterialCategory())
           .orderByDesc(Material::getCreatedAt);

    Page<Material> page = materialMapper.selectPage(
        new Page<>(query.getPageNum(), query.getPageSize()), wrapper);

    return PageResult.of(page, MaterialVO.class);
}
```

### 2.4 认证框架选型

#### 2.4.1 JWT (JSON Web Token)

**选型理由**

| 维度 | 选型理由 |
|---|---|
| **无状态认证** | JWT自包含用户信息，无需服务端Session存储，适配微服务架构 |
| **跨服务传递** | Token可在网关解析后传递给下游服务，避免服务间认证耦合 |
| **API规范适配** | 完美适配API设计文档中的认证规范（Authorization: Bearer {token}） |
| **多端支持** | 支持Web端、移动端、后厨操作端多端认证 |

**业务适配场景**

| 业务场景 | JWT应用 |
|---|---|
| 用户登录 | 登录成功返回accessToken、refreshToken、expiresIn |
| 接口鉴权 | 网关统一校验Token，解析用户身份（userId、orgId、tenantId） |
| Token刷新 | refreshToken机制续期，避免频繁登录 |
| 权限校验 | Token中携带角色和权限信息，支持RBAC权限控制 |

**JWT Payload设计**

```json
{
  "sub": "1",                    // 用户ID
  "username": "admin",           // 用户名
  "realName": "系统管理员",       // 真实姓名
  "orgId": 1001,                 // 所属组织ID
  "tenantId": 1,                 // 租户ID
  "roles": ["admin"],            // 角色列表
  "permissions": [               // 权限列表
    "scm:supplier:create",
    "scm:supplier:edit"
  ],
  "iat": 1710691200,             // 签发时间
  "exp": 1710698400              // 过期时间
}
```

### 2.5 工具库选型

#### 2.5.1 Hutool

**选型理由**

| 维度 | 选型理由 |
|---|---|
| **工具丰富** | 提供字符串、日期、加密、HTTP、JSON等常用工具类 |
| **开发效率** | 减少重复造轮子，提升开发效率 |
| **国内适配** | 身份证、手机号、银行卡等国内特有工具支持 |

**业务应用场景**

| 业务场景 | Hutool工具类 |
|---|---|
| 日期处理 | DateUtil处理入出库日期、保质期计算 |
| 字符串处理 | StrUtil处理物料编码、订单编号生成 |
| 加密解密 | SecureUtil处理密码加密、Token签名 |
| HTTP调用 | HttpUtil实现服务间HTTP调用 |
| JSON处理 | JSONUtil处理设备配置JSON字段 |

#### 2.5.2 Lombok

**选型理由**

| 维度 | 选型理由 |
|---|---|
| **代码简洁** | 自动生成getter/setter/toString等方法，减少样板代码 |
| **配合MyBatis Plus** | @Data、@Builder注解简化Entity和DTO定义 |
| **日志集成** | @Slf4j注解自动注入日志对象 |

### 2.6 后端技术栈选型汇总

| 组件类型 | 选型 | 版本 | 选型理由 | 业务适配场景 |
|---|---|---|---|---|
| 核心框架 | Spring Boot | 3.2.x | 微服务架构、云原生适配、Java 17基线 | 支撑采购、仓储、菜谱、烹饪等模块独立部署 |
| 微服务套件 | Spring Cloud | 2023.0.x | 服务注册、配置中心、网关路由 | 实现服务间HTTP通信、Nacos注册发现 |
| 服务注册 | Nacos | 2.3.x | 阿里巴巴开源、Spring Cloud Alibaba原生支持 | 配置中心地址：192.168.31.4:8848 |
| API网关 | Spring Cloud Gateway | 4.x | 响应式编程、路由转发、鉴权限流 | 统一入口`/api/v1/**`，服务路由分发 |
| ORM框架 | MyBatis Plus | 3.5.x | 自动CRUD、条件构造器、分页插件 | 对接API设计文档的接口参数，简化数据访问 |
| 数据库 | MySQL | 8.0 | 成熟稳定、MyBatis Plus适配、共享数据库架构 | 支撑盘点表、食材表等核心表的存储 |
| 认证框架 | JWT | - | 无状态认证、跨服务传递、多端支持 | 所有接口的权限校验，避免服务间认证耦合 |
| 工具库 | Hutool | 5.8.x | 工具丰富、开发效率高、国内适配 | 日期处理、加密解密、HTTP调用 |
| 代码简化 | Lombok | 1.18.x | 减少样板代码、配合MyBatis Plus | Entity、DTO定义简化 |

---

## 3. 前端技术栈选型与论证

### 3.1 核心框架选型

#### 3.1.1 Vue 3 + Vite

**选型理由**

| 维度 | 选型理由 |
|---|---|
| **构建速度** | Vite基于ESM的HMR，开发启动速度比Webpack快10倍+ |
| **Composition API** | Vue 3 Composition API提供更好的逻辑复用和代码组织 |
| **Element Plus适配** | Element Plus完美适配Vue 3，提供丰富的企业级组件 |
| **Three.js集成** | 支持Three.js 3D渲染，满足设备3D可视化需求 |

**业务适配性**

| 业务场景 | Vue 3 + Vite应用 |
|---|---|
| 数据看板 | Composition API封装数据看板逻辑，实现代码复用 |
| 表单管理 | Reactive响应式数据管理，简化表单状态维护 |
| 设备3D可视化 | Three.js集成，实现设备3D模型展示和交互 |
| 路由管理 | Vue Router 4.x支持路由懒加载，优化首屏加载 |

**技术优势**

```
┌─────────────────────────────────────────────────────────────┐
│                    Vue 3 + Vite 核心优势                      │
├─────────────────────────────────────────────────────────────┤
│ 1. 开发体验优化                                               │
│    - Vite秒级冷启动                                           │
│    - HMR即时热更新                                            │
│    - 按需编译，不等待整个项目构建                              │
│                                                              │
│ 2. Composition API优势                                        │
│    - 逻辑复用（提取可复用逻辑到composables）                   │
│    - 类型推断（配合TypeScript）                               │
│    - 代码组织（按功能组织而非选项）                            │
│                                                              │
│ 3. 性能优化                                                   │
│    - Tree-shaking优化                                         │
│    - 静态节点提升                                             │
│    - 更小的打包体积                                           │
│                                                              │
│ 4. 生态适配                                                   │
│    - Pinia状态管理（替代Vuex）                                │
│    - Element Plus组件库                                       │
│    - VueUse工具库                                             │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 UI组件库选型

#### 3.2.1 Element Plus

**选型理由**

| 维度 | 选型理由 |
|---|---|
| **Vue 3原生支持** | Element Plus专为Vue 3设计，提供完整的TypeScript类型定义 |
| **组件丰富** | 提供60+企业级组件，覆盖表格、表单、弹窗、导航等场景 |
| **主题定制** | 支持SCSS变量定制，适配企业品牌色 |
| **中文友好** | 完善的中文文档和社区支持 |

**业务适配场景**

| 业务模块 | Element Plus组件应用 |
|---|---|
| 列表页面 | el-table + el-pagination实现分页列表 |
| 表单页面 | el-form + el-input + el-select + el-date-picker |
| 数据看板 | el-card + el-statistic + el-progress |
| 树形结构 | el-tree实现组织树、菜单树 |
| 文件上传 | el-upload实现附件上传（入库单、留样附件） |
| 对话框 | el-dialog实现新增/编辑弹窗 |

**代码示例**

```vue
<template>
  <!-- 物料列表页面 -->
  <el-card>
    <!-- 搜索表单 -->
    <el-form :model="queryParams" inline>
      <el-form-item label="物料名称">
        <el-input v-model="queryParams.materialName" placeholder="请输入物料名称" />
      </el-form-item>
      <el-form-item label="物料类别">
        <el-select v-model="queryParams.materialCategory" placeholder="请选择">
          <el-option label="食材" value="食材" />
          <el-option label="调料" value="调料" />
        </el-select>
      </el-form-item>
      <el-form-item>
        <el-button type="primary" @click="handleQuery">查询</el-button>
        <el-button @click="handleReset">重置</el-button>
      </el-form-item>
    </el-form>

    <!-- 数据表格 -->
    <el-table :data="tableData" v-loading="loading">
      <el-table-column prop="materialCode" label="物料编码" />
      <el-table-column prop="materialName" label="物料名称" />
      <el-table-column prop="materialSpec" label="规格" />
      <el-table-column prop="stockQty" label="库存数量" />
      <el-table-column prop="stockStatus" label="库存状态">
        <template #default="{ row }">
          <el-tag :type="getStockStatusType(row.stockStatus)">
            {{ row.stockStatus }}
          </el-tag>
        </template>
      </el-table-column>
      <el-table-column label="操作" width="200">
        <template #default="{ row }">
          <el-button link type="primary" @click="handleEdit(row)">编辑</el-button>
          <el-button link type="danger" @click="handleDelete(row)">删除</el-button>
        </template>
      </el-table-column>
    </el-table>

    <!-- 分页 -->
    <el-pagination
      v-model:current-page="queryParams.pageNum"
      v-model:page-size="queryParams.pageSize"
      :total="total"
      :page-sizes="[10, 20, 50, 100]"
      layout="total, sizes, prev, pager, next, jumper"
      @size-change="handleQuery"
      @current-change="handleQuery"
    />
  </el-card>
</template>

<script setup>
import { ref, reactive, onMounted } from 'vue'
import { getMaterialList } from '@/api/wms/material'

// 查询参数
const queryParams = reactive({
  pageNum: 1,
  pageSize: 20,
  materialName: '',
  materialCategory: ''
})

// 表格数据
const tableData = ref([])
const total = ref(0)
const loading = ref(false)

// 查询列表
const handleQuery = async () => {
  loading.value = true
  try {
    const { data } = await getMaterialList(queryParams)
    tableData.value = data.list
    total.value = data.total
  } finally {
    loading.value = false
  }
}

// 库存状态标签类型
const getStockStatusType = (status) => {
  const typeMap = {
    '正常': 'success',
    '库存不足': 'warning',
    '库存积压': 'info',
    '已过期': 'danger'
  }
  return typeMap[status] || 'info'
}

onMounted(() => {
  handleQuery()
})
</script>
```

### 3.3 状态管理选型

#### 3.3.1 Pinia

**选型理由**

| 维度 | 选型理由 |
|---|---|
| **Vue 3原生** | Pinia专为Vue 3设计，提供完整的TypeScript支持 |
| **轻量化** | 相比Vuex更轻量，无mutations概念，代码更简洁 |
| **DevTools支持** | 完善的Vue DevTools调试支持 |
| **API响应适配** | 完美匹配API响应体的data字段解析 |

**业务适配场景**

| 业务场景 | Pinia Store设计 |
|---|---|
| 用户信息 | useUserStore存储用户信息、权限列表 |
| 组织选择 | useOrgStore存储当前选中组织 |
| 菜单权限 | usePermissionStore存储菜单树、权限点 |
| 数据字典 | useDictStore缓存数据字典 |

**代码示例**

```javascript
// stores/user.js
import { defineStore } from 'pinia'
import { login, getUserInfo, logout } from '@/api/auth'

export const useUserStore = defineStore('user', {
  state: () => ({
    token: localStorage.getItem('accessToken') || '',
    userInfo: null,
    permissions: []
  }),

  getters: {
    isLoggedIn: (state) => !!state.token,
    hasPermission: (state) => (permission) => {
      return state.permissions.includes(permission)
    }
  },

  actions: {
    // 登录
    async login(loginForm) {
      const { data } = await login(loginForm)
      this.token = data.accessToken
      localStorage.setItem('accessToken', data.accessToken)
      localStorage.setItem('refreshToken', data.refreshToken)
      return data
    },

    // 获取用户信息
    async getUserInfo() {
      const { data } = await getUserInfo()
      this.userInfo = data
      this.permissions = data.permissions
      return data
    },

    // 登出
    async logout() {
      await logout()
      this.token = ''
      this.userInfo = null
      this.permissions = []
      localStorage.removeItem('accessToken')
      localStorage.removeItem('refreshToken')
    }
  }
})
```

### 3.4 HTTP客户端选型

#### 3.4.1 Axios

**选型理由**

| 维度 | 选型理由 |
|---|---|
| **拦截器机制** | 请求/响应拦截器实现统一Token注入、错误处理 |
| **请求取消** | 支持CancelToken取消请求，优化组件卸载时的请求处理 |
| **并发请求** | 支持并发请求，适配数据看板多接口聚合场景 |
| **响应适配** | 完美匹配API统一响应格式`{code, message, data, traceId, timestamp}` |

**业务适配场景**

| 业务场景 | Axios应用 |
|---|---|
| Token注入 | 请求拦截器自动注入Authorization头 |
| 响应解析 | 响应拦截器统一解析code、message、data |
| 错误处理 | 响应拦截器统一处理401、403、500等错误 |
| 请求追踪 | 请求拦截器注入traceId，响应拦截器记录日志 |

**代码示例**

```javascript
// utils/request.js
import axios from 'axios'
import { ElMessage } from 'element-plus'
import { useUserStore } from '@/stores/user'

// 创建axios实例
const request = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL || '/api/v1',
  timeout: 30000
})

// 请求拦截器
request.interceptors.request.use(
  (config) => {
    const userStore = useUserStore()
    if (userStore.token) {
      config.headers.Authorization = `Bearer ${userStore.token}`
    }
    // 注入traceId
    config.headers['X-Trace-Id'] = generateTraceId()
    return config
  },
  (error) => {
    return Promise.reject(error)
  }
)

// 响应拦截器
request.interceptors.response.use(
  (response) => {
    const { code, message, data } = response.data

    // 成功响应
    if (code === 'SUCCESS') {
      return response.data
    }

    // 业务错误
    ElMessage.error(message || '请求失败')
    return Promise.reject(new Error(message))
  },
  (error) => {
    const { response } = error

    if (response) {
      switch (response.status) {
        case 401:
          // Token过期，跳转登录
          const userStore = useUserStore()
          userStore.logout()
          window.location.href = '/login'
          break
        case 403:
          ElMessage.error('无权限访问')
          break
        case 500:
          ElMessage.error('服务器错误')
          break
        default:
          ElMessage.error(response.data?.message || '请求失败')
      }
    } else {
      ElMessage.error('网络错误')
    }

    return Promise.reject(error)
  }
)

export default request
```

### 3.5 3D渲染选型

#### 3.5.1 Three.js

**选型理由**

| 维度 | 选型理由 |
|---|---|
| **行业标准** | Three.js是Web 3D渲染的事实标准，生态完善 |
| **Vue集成** | 支持@react-three/fiber类似的Vue封装库（如TroisJS） |
| **设备可视化** | 支持设备3D模型导入、交互、动画 |
| **性能优化** | 支持WebGL渲染，性能优异 |

**业务适配场景**

| 业务场景 | Three.js应用 |
|---|---|
| 设备3D展示 | 展示温度传感器、气体监测设备等3D模型 |
| 设备交互 | 点击设备查看详情、状态变化动画 |
| 厨房布局 | 3D展示厨房布局、设备位置 |
| 实时数据 | 设备温度、状态等实时数据3D可视化 |

### 3.6 前端技术栈选型汇总

| 组件类型 | 选型 | 版本 | 选型理由 | 业务适配场景 |
|---|---|---|---|---|
| 核心框架 | Vue | 3.x | Composition API、性能优化、生态完善 | 数据看板、表单管理、路由管理 |
| 构建工具 | Vite | 5.x | 极速开发体验、ESM原生支持 | 开发环境启动、生产构建 |
| UI组件库 | Element Plus | 2.x | Vue 3原生支持、组件丰富、中文友好 | 表单、表格、弹窗、导航等高频组件 |
| 状态管理 | Pinia | 2.x | 轻量化、TypeScript支持、DevTools支持 | 用户信息、权限、组织选择等全局状态 |
| HTTP客户端 | Axios | 1.x | 拦截器、请求取消、并发请求 | API接口调用、Token注入、错误处理 |
| 3D渲染 | Three.js | 0.160.x | 行业标准、WebGL支持、生态完善 | 设备3D可视化、厨房布局展示 |
| 路由管理 | Vue Router | 4.x | Vue 3原生支持、路由懒加载 | 页面路由、权限控制 |
| 样式方案 | SCSS | - | 变量、嵌套、混入 | 主题定制、样式复用 |

---

## 4. 部署技术栈选型

### 4.1 Docker Compose

**选型理由**

| 维度 | 选型理由 |
|---|---|
| **统一管理** | Docker Compose统一管理前后端服务、数据库、Redis等容器 |
| **开发便利** | 本地开发一键启动所有服务，简化开发环境搭建 |
| **健康检查** | 支持容器健康检查，自动重启异常服务 |
| **网络隔离** | 支持自定义网络，服务间通过容器名访问 |

**适配性说明**

| 配置项 | 配置值 | 说明 |
|---|---|---|
| Nacos地址 | 192.168.31.4:8848 | 配置中心、服务注册地址 |
| MySQL端口 | 3306 | 数据库服务端口 |
| Redis端口 | 6379 | 缓存服务端口 |
| 后端JVM参数 | Xms256m -Xmx512m | 适配容器资源限制 |

### 4.2 Nginx

**选型理由**

| 维度 | 选型理由 |
|---|---|
| **静态资源服务** | 高效服务前端静态资源（HTML、CSS、JS） |
| **反向代理** | 代理后端API请求，解决跨域问题 |
| **负载均衡** | 支持多实例负载均衡 |
| **缓存优化** | 静态资源缓存，优化访问性能 |

**配置示例**

```nginx
# nginx.conf
upstream backend {
    server scm-service:8080;
    server wms-service:8080;
    server recipe-service:8080;
    server cook-service:8080;
}

server {
    listen 80;
    server_name localhost;

    # 前端静态资源
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # API代理
    location /api/ {
        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Trace-Id $request_id;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
        root /usr/share/nginx/html;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}
```

### 4.3 部署架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                            用户浏览器                                     │
│                    (Web端/移动端/后厨操作端)                              │
└────────────────────────────────┬────────────────────────────────────────┘
                                 │ HTTPS
                                 ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                              Nginx                                       │
│                    (静态资源服务 + API反向代理)                           │
└────────────────────────────────┬────────────────────────────────────────┘
                                 │
        ┌────────────────────────┼────────────────────────┐
        │                        │                        │
        ▼                        ▼                        ▼
┌───────────────┐     ┌───────────────────┐     ┌───────────────┐
│   Spring      │     │   Spring Cloud    │     │   Nacos       │
│   Cloud       │     │   Gateway         │     │   192.168.31.4│
│   Gateway     │◄───►│   :8080           │     │   :8848       │
└───────┬───────┘     └───────────────────┘     └───────────────┘
        │
        │ /api/v1/scm/** → scm-service
        │ /api/v1/wms/** → wms-service
        │ /api/v1/recipe/** → recipe-service
        │ /api/v1/cook/** → cook-service
        │ /api/v1/sample/** → sample-service
        │ /api/v1/health/** → health-service
        │ /api/v1/device/** → device-service
        │
        └──────────────────────┬────────────────────────────────┘
                               │
    ┌──────────────────────────┼──────────────────────────┐
    │                          │                          │
    ▼                          ▼                          ▼
┌─────────┐            ┌─────────────┐            ┌─────────────┐
│ MySQL   │            │ Redis       │            │ MinIO/OSS   │
│ :3306   │            │ :6379       │            │ (文件存储)   │
│         │            │             │            │             │
│ 共享数据库           │ Token缓存   │            │ 附件、图片   │
│ 表前缀隔离           │ 会话管理    │            │             │
└─────────┘            └─────────────┘            └─────────────┘
```

---

## 5. 技术栈适配规范

### 5.1 后端开发规范

#### 5.1.1 Java 17规范

| 规范项 | 要求 | 示例 |
|---|---|---|
| Records使用 | DTO/VO使用Records简化定义 | `public record UserVO(Long id, String username) {}` |
| Sealed Classes | 领域模型使用Sealed限制继承 | `public sealed interface OrderStatus permits Pending, Completed, Cancelled {}` |
| Pattern Matching | 使用模式匹配简化业务逻辑 | `if (obj instanceof String s) { ... }` |
| Text Blocks | SQL、JSON使用文本块 | `String sql = """ SELECT * FROM users """;` |

#### 5.1.2 RESTful API规范

| 规范项 | 要求 | 示例 |
|---|---|---|
| URL命名 | 小写、连字符分隔 | `/api/v1/purchase-orders` |
| HTTP方法 | 正确使用GET/POST/PUT/DELETE | GET查询、POST创建、PUT更新、DELETE删除 |
| 状态码 | 正确使用HTTP状态码 | 200成功、201创建、400参数错误、401未认证、403无权限、404不存在、500服务器错误 |
| 响应格式 | 统一响应体格式 | `{code, message, data, traceId, timestamp}` |

#### 5.1.3 Hutool/Lombok使用规范

| 规范项 | 要求 | 示例 |
|---|---|---|
| 日期处理 | 使用Hutool DateUtil | `DateUtil.parse(dateStr, "yyyy-MM-dd")` |
| 字符串处理 | 使用Hutool StrUtil | `StrUtil.isBlank(str)` |
| Entity定义 | 使用@Data、@Builder注解 | `@Data @Builder public class Material { ... }` |
| 日志使用 | 使用@Slf4j注解 | `@Slf4j public class MaterialService { log.info("..."); }` |

### 5.2 前端开发规范

#### 5.2.1 Vue 3 Composition API规范

| 规范项 | 要求 | 示例 |
|---|---|---|
| 组件定义 | 使用`<script setup>`语法 | `<script setup> import { ref } from 'vue' </script>` |
| 响应式数据 | 使用ref/reactive | `const count = ref(0)` |
| 逻辑复用 | 提取composables | `const { list, loading } = useList()` |
| Props定义 | 使用defineProps | `const props = defineProps<{ id: number }>()` |

#### 5.2.2 SCSS规范

| 规范项 | 要求 | 示例 |
|---|---|---|
| 变量命名 | 使用`$`前缀 | `$primary-color: #409EFF;` |
| 嵌套层级 | 不超过3层 | `.card { .header { .title { } } }` |
| 混入使用 | 复用样式片段 | `@mixin flex-center { display: flex; align-items: center; }` |
| BEM命名 | 推荐BEM命名规范 | `.card__header--active` |

### 5.3 微服务规范

#### 5.3.1 服务间通信规范

| 规范项 | 要求 | 说明 |
|---|---|---|
| 通信方式 | HTTP RESTful | 服务间通过HTTP调用，避免RPC强耦合 |
| 服务发现 | Nacos注册发现 | 服务注册到Nacos，通过服务名调用 |
| 链路追踪 | traceId传递 | 所有请求携带traceId，支持全链路追踪 |

#### 5.3.2 共享数据库隔离规范

| 规范项 | 要求 | 示例 |
|---|---|---|
| 表名前缀 | 按服务模块添加前缀 | scm_采购模块、wms_仓储模块、recipe_菜谱模块 |
| 读写分离 | 主从读写分离 | 写操作访问主库，读操作访问从库 |
| 数据权限 | tenant_id + org_id | 所有表包含tenant_id、org_id字段，实现多租户数据隔离 |

---

## 6. 附录

### 6.1 技术栈版本汇总表

| 分类 | 组件 | 版本 | 说明 |
|---|---|---|---|
| **后端** | Java | 17 | 运行时环境 |
| | Spring Boot | 3.2.x | 核心框架 |
| | Spring Cloud | 2023.0.x | 微服务套件 |
| | Spring Cloud Alibaba | 2023.0.x | 阿里巴巴微服务组件 |
| | Nacos | 2.3.x | 配置中心/注册中心 |
| | Spring Cloud Gateway | 4.x | API网关 |
| | MyBatis Plus | 3.5.x | ORM框架 |
| | MySQL | 8.0 | 数据库 |
| | JWT | - | 认证框架 |
| | Hutool | 5.8.x | 工具库 |
| | Lombok | 1.18.x | 代码简化 |
| **前端** | Vue | 3.x | 核心框架 |
| | Vite | 5.x | 构建工具 |
| | Element Plus | 2.x | UI组件库 |
| | Pinia | 2.x | 状态管理 |
| | Axios | 1.x | HTTP客户端 |
| | Three.js | 0.160.x | 3D渲染 |
| | Vue Router | 4.x | 路由管理 |
| | SCSS | - | 样式方案 |
| **部署** | Docker | 24.x | 容器运行时 |
| | Docker Compose | 2.x | 容器编排 |
| | Nginx | 1.24.x | 反向代理 |

### 6.2 参考文档

| 文档名称 | 版本 | 说明 |
|---|---|---|
| 智慧厨房管理平台 -标准PRD | v1.0 | 业务需求规格 |
| 智能食安管理平台功能清单 | v1.0 | 功能模块清单 |
| 数据库设计-智慧厨房管理平台 | v1.0 | 数据库表结构设计 |
| API设计文档-智慧厨房管理平台 | v1.0 | API接口设计 |

---

**版本**: v1.0
**创建日期**: 2026-03-17
**最后更新**: 2026-03-17