# 1) 进入项目根目录

cd /Users/guanyiru/Desktop/yingzicode/yingzi-zncf

# 2) 确保 mysql 容器在线（不在线就先拉起）

docker start kitchen-mysql >/dev/null 2>&1 || true

# 3) 仅重置并重灌 recipe 相关表到 smart_kitchen（推荐，影响面最小）

perl -pe 's/USE `smart_food_safety`;/USE `smart_kitchen`;/g' \
  backend/sql/init/04-recipe-tables.sql \
| docker exec -i kitchen-mysql mysql -uroot -p'Root@123'

# 4) 验证表是否存在

docker exec kitchen-mysql mysql -uroot -p'Root@123' -D smart_kitchen -e "SHOW TABLES LIKE 'recipe%';"

# 5) 验证后端接口

curl -sS "http://localhost:8084/api/v1/recipe/dashboard/overview?timeRange=today"

# 6) 验证前端代理联调

curl -sS "http://localhost:5173/api/v1/recipe/dashboard/overview?timeRange=today"
